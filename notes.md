# 학습 노트

> 챕터 1~7 전체를 다이어그램·코드 예시와 함께 정리한 HTML 자료는 `index.html`(학습 지도)부터 시작 — 부품편(Ch1-3) → 조립편(Ch4-6) → 노선도(Ch7) 순.

## Phase 1 (7.1) 멀티 에이전트 4가지 패턴

핵심 질문: **"누가 다음에 실행할 노드를 결정하는가?"**

| 패턴 | 라우팅 결정자 | 통신 구조 | 적합한 상황 |
|---|---|---|---|
| 네트워크 | 각 에이전트 스스로 | 서로 직접 핸드오프 (P2P) | 에이전트 수가 적고 역할 경계가 유동적일 때 |
| 슈퍼바이저 | 감독 에이전트 1개 | 하위 에이전트는 도구처럼 호출됨 | 역할이 명확히 나뉘고 중앙 조율이 필요할 때 |
| 계층형 | 여러 단의 슈퍼바이저 | 슈퍼바이저의 슈퍼바이저 (재귀적) | 팀/에이전트 수가 아주 많을 때 |
| 커스텀 | 상황에 따라 혼합 | 패턴 조합 | 실무 요구사항이 정형 패턴에 안 맞을 때 |

### 네트워크 패턴 (7.1.1)
- 에이전트끼리 `Command`로 직접 핸드오프. 중앙 조율자 없음.
- 단점: 에이전트가 늘어나면 "누가 누구를 부를 수 있는지" 관계가 복잡해짐.
- 책 예시(Phase 3): 정보 검색 에이전트 ⇄ 차트 생성 에이전트.

### 슈퍼바이저 패턴 (7.1.2)
- 슈퍼바이저가 하위 에이전트를 **도구(tool)처럼** 호출. 하위 에이전트끼리는 서로 모름.
- 항상 슈퍼바이저를 거쳐 결과가 오감(spoke-and-hub).
- 책 예시(Phase 4): 웹 분석 → DB 관리 순차 위임.
- 책 예시(Phase 5): 최신검색/내부DB/FAQ 3개 중 질문 유형별 라우팅.

### 계층형 패턴 (7.1.3)
- 슈퍼바이저 패턴을 재귀적으로 중첩(슈퍼바이저의 슈퍼바이저).
- 각 계층은 자기 하위 계층만 알면 됨 → 대규모 확장에 유리, 대신 지연 증가.
- 책 예시(Phase 6, 플래닝 기반 슈퍼바이저): Planner가 계획 수립 → 슈퍼바이저가 계획대로 하위 에이전트 반복 호출.

### 커스텀 (7.1.4)
- 패턴 이름보다 "라우팅 결정 권한"과 "state 공유 방식"을 상황에 맞게 설계하는 게 핵심.
- 실무에서는 슈퍼바이저 밑 하위 에이전트끼리 네트워크처럼 직접 핸드오프하는 등 혼합 사용.

**체크포인트 통과 기준**: 네트워크 패턴은 검색 에이전트가 스스로 판단해 차트 에이전트로 핸드오프하지만, 슈퍼바이저 패턴은 검색 에이전트가 결과를 슈퍼바이저에게 돌려주고 슈퍼바이저가 다시 판단해 차트 에이전트를 호출 — 이 차이(직접 핸드오프 vs 항상 중앙 경유)가 핵심.

---

## Phase 0 완료 기록
- `.venv` 생성, `langgraph`(1.2.11) `langchain-openai` `langchain-tavily` `python-dotenv` 설치 완료.
- `.env.example` / `.gitignore` / `requirements.txt` 작성 완료.
- API 키(OPENAI_API_KEY, TAVILY_API_KEY)는 사용자가 `.env`에 직접 채워야 함.

---

## Phase 2 (7.2) 핸드오프 — Command (이론 정리 완료, 실습은 아직)

- 조건부 엣지와의 차이: 조건부 엣지는 **다음 노드 이름만** 결정하지만, `Command`는 **다음 노드 + 상태 업데이트를 동시에** 반환한다.
- 최소 예제: `Command(goto="agent_2", update={"messages": [...]})` — `add_edge`는 시작 엣지(`START → agent_1`)만 있으면 되고, 나머지 이동은 노드 함수 안에서 Command로 처리.
- 조건부 Command: 노드 함수 리턴 타입에 `Command[Literal["agent_2", "agent_3"]]`처럼 갈 수 있는 노드를 명시하고, 함수 내부 `if`문으로 상태값에 따라 다른 `goto`를 반환.
- 실제로 손으로 코드를 쳐보는 실습(`phase2_handoff_basics/`)은 아직 안 함 — 이론만 정리된 상태. 자세한 코드는 `study-guide.html`의 Phase 2 섹션 참고.
